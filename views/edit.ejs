<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>User Profile</title>
</head>
<body class="bg-gray-900 flex flex-col items-center min-h-screen py-12 px-4">
   
<%- include('partials/navbar') %>

    <!-- Post Creation Form -->
    <div class="bg-gray-800 p-8 rounded-xl shadow-xl w-full max-w-lg mb-10 transition-all duration-300 hover:shadow-2xl">
        <h2 class="text-2xl font-bold text-white mb-6">Edit a Post</h2>
        <form action="/update/<%= posts._id%>" method="POST" class="space-y-6">
            <textarea             
                name="content" 
                placeholder="What's on your mind?" 
                class="w-full px-4 py-3 bg-gray-700 text-white placeholder-gray-400 border border-gray-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-teal-500 resize-y h-32 transition-all duration-200"
                required
            ><%= posts.content %></textarea>
            <button 
                type="submit" 
                class="w-full px-4 py-3 bg-teal-500 text-white font-semibold rounded-lg hover:bg-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 transition-colors duration-200"
            >
                Post
            </button>
        </form>
    </div>

   
</body>
</html>